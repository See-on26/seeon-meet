# 의존성
node_modules/

# 빌드·실행 산출물
dist/
out/

# 환경변수 — 실제 키가 들어간다. 커밋 금지 (템플릿은 .env.example)
.env
.env.*
!.env.example

# 대용량 미디어·부가 자료 (저장소에 올리지 않는다)
*.mp4
*.mov
*.xlsx
demo-video-maker/

# 에디터·OS
.DS_Store
.idea/
.vscode/
